# Startup and Resource Performance Plan

1. Add failing source-boundary and lifecycle cleanup tests.
2. Move editor runtime imports into the presentation `open()` path.
3. Revoke late thumbnail blob URLs and clean up Whisper progress listeners on unmount.
4. Remove duplicate per-window shortcut watcher registration.
5. Run focused tests and the full verification suite.
