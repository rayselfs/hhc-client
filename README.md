# HHC Presenter

HHC Presenter is a projection system designed for church services. It focuses on reliable
projection, quick media playback, Bible display, timers, and local-first operation.

## Features

- Dual-window projection for a control screen and a dedicated output screen
- Media library with folders, thumbnails, trash, and offline-ready native files
- Image, PDF, and video presentation
- PPTX media items with browser-native preview, workspace tabs, and projection
- Desktop video playback through bundled VLC/libVLC
- Desktop video posters through bundled FFmpeg poster generation
- Web mode with browser-native video playback and thumbnails
- Bible display and timer tools
- Local-first storage using IndexedDB and Electron native file storage

## Desktop vs Web

| Capability                      | Desktop             | Web                 |
| ------------------------------- | ------------------- | ------------------- |
| Image projection                | Yes                 | Yes                 |
| PDF projection                  | Yes                 | Yes                 |
| PPTX preview and projection     | Yes                 | Yes                 |
| Browser-native video formats    | Yes                 | Yes                 |
| MKV / AVI / WMV playback        | Yes, via VLC/libVLC | No                  |
| Video poster generation         | Yes, via FFmpeg     | Browser-native only |
| Native filesystem media storage | Yes                 | No                  |
| IndexedDB media storage         | Legacy fallback     | Yes                 |

## Development

```bash
npm install
npm run dev
```

Useful checks:

```bash
npm run lint
npm run typecheck
npm test
npm run build
```

Desktop packages:

```bash
npm run build:mac
npm run build:win
```

Desktop packages require local video engine binaries before packaging. Keep them
outside git under `.local-runtimes/`, then run the package command:

```text
.local-runtimes/
  vlc/darwin-arm64/
  vlc/darwin-x64/
  vlc/win32-x64/
  ffmpeg/darwin-arm64/ffmpeg
  ffmpeg/darwin-x64/ffmpeg
  ffmpeg/win32-x64/ffmpeg.exe
```

`npm run build` does not require these binaries. `npm run build:unpack` and
desktop packaging run `prepare:video-engine:strict` and fail if the current
platform runtime is missing. `npm run build:unpack` also verifies that the
unpacked app contains the bundled video runtime and license notices.

Release packaging is currently unsigned. See
[`docs/release/unsigned-desktop-releases.md`](docs/release/unsigned-desktop-releases.md)
for GitHub Actions runtime archive variables, OS warning expectations, and the
future signing/notarization path.

## Architecture

HHC Presenter is an Electron + React + TypeScript app.

- `src/main/`: Electron main process, windows, IPC, native protocols
- `src/preload/`: typed renderer bridge
- `src/renderer/src/`: React UI, stores, media services, projection screens
- `src/shared/`: shared IPC contracts and domain types

Desktop video playback uses bundled VLC/libVLC. Desktop video posters are
generated by bundled FFmpeg in poster-only mode; HHC Presenter does not expose
FFmpeg transcoding settings to users. Web mode keeps using browser-native media
APIs and does not support desktop-only containers such as MKV, AVI, or WMV.
PPTX files are treated as media items and rendered in the browser runtime through
a shared presentation workspace; V1 supports page-level preview, projection, and
workspace tabs rather than full PowerPoint element editing.

## License

HHC Presenter is licensed under GPL-3.0-or-later.

Third-party notices are documented in `THIRD_PARTY_NOTICES.md`.

## Contributing

Keep changes small, typed, and easy to review. Run lint, typecheck, tests, and
build before opening a pull request.

## Security

Please report security issues privately before public disclosure.
